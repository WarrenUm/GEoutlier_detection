[pytest]
# Discover the per-lesson notebook tests under curriculum/.
testpaths = curriculum
python_files = test_*.py
# Every lesson has a test_work.py with the same basename; importlib mode lets
# pytest import them all without name collisions (no __init__.py needed).
addopts = -ra --import-mode=importlib
